<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="CustomerAccountDAO">

<!-- 계좌정보 조회 -->
<select id="getCustomerAccount" parameterType="String" resultType="customerAccount">
	select m_id,m_acc_num,m_acc_bank from member_acc where m_id = #{id}
</select>

<update id="updateAccount" parameterType="customerAccountInfo">	
	update member_acc set m_acc_bank = #{m_acc_bank} ,m_acc_num = #{m_acc_num}
	where m_id = #{m_id}
</update>

</mapper>