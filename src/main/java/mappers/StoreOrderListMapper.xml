<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 유승민 사용 매퍼 -->
<mapper namespace="storeOrderListDao">

<select id="getStoreOrderList" parameterType="String" resultType="storeOrderList">

	select
	o_code,
	o_content,
	m_id,
	to_char(o_date,'yyyy-mm-dd') as o_date,
	s_id 
	from orderinfo where s_id = #{storeID}

</select>

<select id="getStoreOrderEtc" parameterType="Integer" resultType="customerEtcList">

	<![CDATA[
	select  
	et.ETC_CODE, 
	et.ETC_SPNAME, 
	o.O_CODE, 
	et.ETC_TYPE_CODE, 
	et.ETC_PPATH, 
	et.ETC_PNAME, 
	et.ETC_PSIZE, 
	et.ETC_CONTENT,
	et.ETC_DATE,
	o.S_ID,
	o.m_ID 
	from orderinfo o, etc et 
	where o.o_code = et.o_code and o.o_code = #{o_code}
	]]>

</select>

</mapper>
